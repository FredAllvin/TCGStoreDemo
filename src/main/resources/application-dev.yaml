spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tcgshop
    username: tcgshop
    password: devpass
  thymeleaf:
    cache: false
  web:
    resources:
      cache:
        period: 0

app:
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin}
